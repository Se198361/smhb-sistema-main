VITE_SUPABASE_URL=https://SUA-URL-PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=SUA_CHAVE_ANON
VITE_DEV_AUTH_BYPASS=true
DATABASE_URL=postgresql://usuario:senha@host/neondb?sslmode=require

# Supabase/Postgres (optional; use pooler for Prisma)
POSTGRES_PRISMA_URL=postgres://postgres:senha@host:6543/postgres?sslmode=require&pgbouncer=true
POSTGRES_URL_NON_POOLING=postgres://postgres:senha@host:5432/postgres?sslmode=require
POSTGRES_USER=postgres
POSTGRES_PASSWORD=senha
POSTGRES_HOST=db.seuprojeto.supabase.co
POSTGRES_DATABASE=postgres

# Map Prisma to use Supabase pooler
# Set DATABASE_URL to POSTGRES_PRISMA_URL in your local .env
# DATABASE_URL=${POSTGRES_PRISMA_URL}

# Supabase keys
VITE_SUPABASE_URL=https://seuprojeto.supabase.co
VITE_SUPABASE_ANON_KEY=seu_anon_key
# Server-only keys (set only in server env, never commit real values)
# SUPABASE_SERVICE_ROLE_KEY=seu_service_role_key
# SUPABASE_JWT_SECRET=seu_jwt_secret
PORT=3001
JWT_SECRET=defina-um-segredo-seguro-aqui
APP_BASE_URL=http://localhost:5173

# SMTP (opcional para envio de e-mail de redefinição)
SMTP_HOST=smtp.seuprovedor.com
SMTP_PORT=587
SMTP_USER=usuario@seuprovedor.com
SMTP_PASS=sua_senha_de_app
SMTP_FROM="SMHB <no-reply@seuprovedor.com>"